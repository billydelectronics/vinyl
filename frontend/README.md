# Vinyl + SvelteKit + Tailwind + Skeleton

- Polished styles with Tailwind + Skeleton plugin.
- Caddy proxies `/` to SvelteKit, `/api/*` to FastAPI, and serves `/covers/*` directly.

## Quickstart
```bash
docker compose up -d
# open http://vinyl.localhost
```
